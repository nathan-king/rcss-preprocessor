{
  "name": "rcss-syntax",
  "displayName": "RCSS Syntax",
  "description": "Lightweight TextMate grammar for RCSS (tokens, vars, presets, nested blocks).",
  "version": "0.0.8",
  "publisher": "local",
  "main": "./extension.js",
  "engines": {
    "vscode": "^1.84.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "activationEvents": [
    "onLanguage:rcss",
    "onCommand:rcss.buildFile"
  ],
  "contributes": {
    "languages": [
      {
        "id": "rcss",
        "aliases": [
          "RCSS"
        ],
        "extensions": [
          ".rcss"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "rcss",
        "scopeName": "source.rcss",
        "path": "./syntaxes/rcss.tmLanguage.json"
      }
    ],
    "commands": [
      {
        "command": "rcss.buildFile",
        "title": "RCSS: Build Current File"
      }
    ],
    "configuration": {
      "title": "RCSS",
      "properties": {
        "rcss.buildOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Automatically run the RCSS build command when saving an .rcss file."
        },
        "rcss.buildCommand": {
          "type": "string",
          "default": "cargo run -p rcss-cli -- build ${file}",
          "description": "Command to build the current RCSS file."
        },
        "rcss.buildCwd": {
          "type": "string",
          "default": "",
          "description": "Optional working directory for the RCSS build command."
        }
      }
    }
  }
}
